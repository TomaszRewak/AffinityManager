<UserControl 
	x:Class="AffinityManager.Views.RuleControl"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
	xmlns:local="clr-namespace:AffinityManager.Views" 
	xmlns:viewmodels="clr-namespace:AffinityManager.ViewModels"
	xmlns:converters="clr-namespace:AffinityManager.Converters"
	mc:Ignorable="d"
	d:DesignHeight="450"
	d:DesignWidth="800"
	d:DataContext="{d:DesignInstance viewmodels:RuleViewModel}">
	<StackPanel Orientation="Horizontal">
		<TextBox Width="150" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
		<TextBox Width="300" Text="{Binding Pattern, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
		<ItemsControl ItemsSource="{Binding Cores}">
			<ItemsControl.ItemsPanel>
				<ItemsPanelTemplate>
					<StackPanel Orientation="Horizontal"/>
				</ItemsPanelTemplate>
			</ItemsControl.ItemsPanel>
			<ItemsControl.ItemTemplate>
				<DataTemplate>
					<local:CoreControl DataContext="{Binding}"/>
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ItemsControl>
		<TextBlock Text="{Binding MatchingProcesses}"/>
		<TextBlock Text="✎" Visibility="{Binding Changed, Converter={converters:BooleanConverter WhenTrue={x:Static Visibility.Visible}, WhenFalse={x:Static Visibility.Collapsed}}}"/>
	</StackPanel>
</UserControl>
